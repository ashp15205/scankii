# Web Search Skill

This skill allows the agent to search the web using an LLM.

## Usage

The user can ask the agent to search the web. The agent must use the api key to authenticate the request to the upstream search provider.
